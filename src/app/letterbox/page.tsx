import LetterCard from '@/components/letterBoxComponents/LetterCard';
import React from 'react';

const page = () => {
  return (
    <div>
      <LetterCard />
    </div>
  );
};

export default page;
